@model IEnumerable<OtelEleven.Models.Kampanya>
@{
    ViewBag.Title = "Kampanyalar";
}

<h2>@ViewBag.Title</h2>
@foreach (var item in Model)
{ 

}

<table class="table"> 
    <tr style="font-weight:bold">
        <td>
            @Resources.Home.Ad
        </td>
        <td>
            @Resources.Home.Baslangic
        </td>
        <td>
             @Resources.Home.Bitis
        </td>
        <td>
            @Resources.Home.İndirim
        </td>
        <td>
            @Resources.Home.Secenekler
        </td>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @item.Ad
            </td>
            <td>
                @item.Baslangic.ToShortDateString()
            </td>
            <td>
                @item.Bitis.ToShortDateString()
            </td>
            <td>
                @item.Indirim
            </td>
            <td> 
                @Html.ActionLink("Details", "Details", new { id = item.Id }) 
            </td>
        </tr>
    }

</table>

